(defun firstItem(a)
  (car a)
  )

(defun secondItem(a)
  (car (cdr a))
  )

(defun lastItem(a)
  (car (last a))
  )

(defun listLenght())

(defun sum())

(defun verify())

(defun invert())

(defun eraseDup())

(defun tenthItem())

(defun concat())

